flask>=3.1,<4
flask-cors>=5.0,<7
requests>=2.32,<3
psycopg2-binary>=2.9,<3
anthropic>=0.80,<1
google-genai>=1.40,<2
PyPDF2>=3.0,<4
pdfplumber>=0.11,<1
python-docx>=1.1,<2
python-pptx>=1.0,<2
pytesseract>=0.3,<1
Pillow>=11.0,<12
google-api-python-client>=2.150,<3
google-auth>=2.40,<3
markdown>=3.7,<4
gunicorn>=23.0,<24
openai>=1.60,<2
img2pdf>=0.5,<1
xhtml2pdf>=0.2,<1
tradingagents @ git+https://github.com/TauricResearch/TradingAgents.git
tavily-python
feedparser>=6.0,<7
beautifulsoup4>=4.12,<5
apscheduler>=3.10,<4
sqlalchemy>=2.0,<3
pytest>=8.0,<9
pytest-flask>=1.3,<2
responses>=0.25,<1
pywebpush>=1.14,<2
youtube-transcript-api>=0.6.2,<2

# Segment donut charts for research notes. Chart generation is wrapped in a
# try/except that only logs, so without these a server-generated note comes out
# silently chartless -- which is what happened when note generation moved off
# the local agent, where matplotlib was already installed.
matplotlib>=3.8,<4
numpy>=1.26,<3

# Excel extraction for research-note sources. Imported inside a try/except, so
# without it a selected .xlsx -- a model, which is a document type the Update
# Config picker offers -- contributes nothing to the note and says nothing.
openpyxl>=3.1,<4

# yfinance is deliberately NOT pinned here.
#
# tradingagents (line above) requires yfinance>=1.4.1, so adding a pin of our
# own makes requirements.txt unresolvable and the Render build fails silently:
# the old process keeps serving and the deploy simply never lands. It is
# available transitively, which is why the import has always worked.

# Used directly for anthropic.Anthropic(timeout=httpx.Timeout(...)). It arrives
# transitively via the Anthropic SDK today, which is not a guarantee: an SDK
# release that swapped its HTTP client would break these call sites with an
# ImportError inside a request handler. Declared because it is used directly.
httpx>=0.27,<1
